# Pointer — download

A local, offline Mac companion that **points at things** to teach you — it never clicks.

Grab the latest build from **[Releases](../../releases/latest)**. Requires macOS 14.2+ (Apple Silicon recommended).
